<%= form_for(agent) do |f| %>
  <% if agent.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(agent.errors.count, "error") %> prohibited this agent from being saved:</h2>

      <ul>
      <% agent.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :full_name %>
    <%= f.text_field :full_name %>
  </div>

  <div class="field">
    <%= f.label :logged %>
    <%= f.check_box :logged %>
  </div>

  <div class="field">
    <%= f.label :call_status %>
    <%= f.check_box :call_status %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
